<nav class="navbar fixed-top navbar-expand-sm navbar-dark bg-dark">
	  <div class="container-fluid ">
		<%= link_to 'RECIPE APP', recipes_path, class:"navbar-brand text-success" %>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		  <span class="navbar-toggler-icon"></span>
		</button>
    <div class="p-2">
      <%= link_to 'Foods', root_path, class:"navbar-brand ml-5 text-warning" %>
      <%= link_to 'Recipes', recipes_path, class:"navbar-brand ml-5 text-warning" %>
    </div>
		<div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav ">
          <% if user_signed_in? %>
            <li class="nav-item d-flex align-items-center">
                <div class="card p-1 m-2">
                  <%= current_user.name.capitalize() %>
                </div>
              <div class="m-2 align-self-end">
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, data: {turbo: false}, class:"btn btn-danger btn-small float-end" %>
              </div>
            </li>
         <% else %>
          <li class="nav-item mx-4">
            <%= link_to "Sign Up", new_user_registration_path, class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign In", new_user_session_path, class:"nav-link btn btn-primary btn-sm" %>
          </li>
         <% end %>
        </ul>		  
	  </div>
</nav>